> **Superseded.** The schema lives in:
> - `sql/schema/007_jobqueue.sql` — all tables, enums, indexes, constraints, triggers
> - `sql/schema/008_jobqueue_functions.sql` — terminal-state protection trigger
>
> No separate schema design doc is needed; the SQL files are self-documenting.
> See `../0-design.md §4` for the design rationale behind each table.
